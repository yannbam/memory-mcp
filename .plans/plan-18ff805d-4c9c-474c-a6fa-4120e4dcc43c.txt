🌳 "fix-critical-errors"
📝 Fix the 3 critical error handling issues found during PR review:
1. Silent directory tree failures in buildDirectoryTree()
2. Misleading "Path not found" errors in exists() helper
3. Missing error handling tests

Using test-first approach with MCP-Debug integration testing.
📊 █████████░ 90% (9/10 tasks)

├── ✅ |Write error handling tests| "Add tests that currently fail, exposing the bugs:\n- Test permission errors in buildDirectoryTree()\n- Test exists() distinguishes ENOENT from EACCES\n- Test per-file stat errors in viewDirectory()" (🚨) (100%)
│   ├── ✅ |Test buildDirectoryTree permission errors| "Write test that creates directory with restricted permissions and verifies error is logged, not silent empty array" (🚨)
│   ├── ✅ |Test exists() distinguishes errors| "Write test that verifies exists() throws for EACCES but returns false for ENOENT" (🚨)
│   └── ✅ |Test viewDirectory per-file errors| "Write test for race condition where file is deleted between readdir and stat" (🚨)
├── ✅ |Fix exists() helper| "Modify exists() to only return false for ENOENT, throw for other errors like EACCES" (🚨)
├── ✅ |Fix buildDirectoryTree() catch block| "Replace empty catch with error logging that distinguishes EACCES, ENOENT, and other errors" (🚨)
├── ✅ |Fix viewDirectory() per-file handling| "Add try-catch around fs.stat() loop to handle per-file errors gracefully" (🚨)
├── ✅ |Run test suite| "Verify all tests pass including new error handling tests" (🔴)
├── ✅ |MCP-Debug integration test| "Test the server with MCP-Debug to verify error handling works in real MCP context" (🔴)
└── 🔄 |Commit fixes| "Create clean commit with error handling fixes and tests" (🟡)
