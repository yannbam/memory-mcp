🌳 "discriminated-union-implementation"
📝 Implement discriminated union schema for memory tool using low-level Server API instead of McpServer.registerTool(). This will provide proper JSON Schema with oneOf and exact required fields per command variant.
📊 ██████████ 100% (8/8 tasks)

├── ✅ |Create schemas.ts| "Create src/memory/schemas.ts with discriminated union for all 6 memory commands. Each command gets its own schema with exact required fields. Export MemoryCommand type and assertNever helper." (🔴)
├── ✅ |Create command-executor.ts| "Create src/memory/command-executor.ts with type-safe dispatch switch statement. Uses discriminated union for automatic type narrowing per command variant." (🔴)
├── ✅ |Refactor mcp-server.ts| "Replace McpServer with low-level Server API. Manually register ListToolsRequestSchema and CallToolRequestSchema handlers. Use zodToJsonSchema to convert discriminated union to proper oneOf JSON Schema." (🔴)
├── ✅ |Update operations.ts| "Update MemoryOperations method signatures to accept discriminated union command objects directly instead of extracting from params. Remove manual command string switches." (🟡)
├── ✅ |Update transports.ts| "Update transport initialization to work with Server instead of McpServer. Change type annotations and ensure server.connect() calls work correctly." (🟡)
├── ✅ |Update index.ts| "Update entry point to import and use the new createMemoryServer function. Ensure all config passing works correctly." (🟡)
├── ✅ |Build and test| "Run npm run build to compile TypeScript. Fix any compilation errors. Verify dist/ output is correct." (🔴)
└── ✅ |Test with Claude Code| "Test the MCP server with actual Claude Code instance via .mcp.json integration. Verify JSON Schema shows proper oneOf structure. Test each command variant." (🚨)
